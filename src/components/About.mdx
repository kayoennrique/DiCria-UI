import { Meta } from "@storybook/blocks"

<Meta title="Molecules/About" />

# Molecules

Molecules are relatively simple groups of UI elements that work together as a unit. For example, a form label, search input and button can come together to create a search field molecule.